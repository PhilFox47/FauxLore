version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - .env:/app/.env
    environment:
      - NODE_ENV=production
      - DATA_DIR=/app/data
